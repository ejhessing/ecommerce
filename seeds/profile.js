
exports.seed = function(knex, Promise) {
  // Deletes ALL existing entries
  // return knex('profile').del()
  //   .then(function () {
  //     return Promise.all([
  //       // Inserts seed entries
  //       knex('profile').insert({id: 601, name: 'aaron', address: "1 somewhere street", city: "auckland", country: "New Zealand", postcode: 4502, user_id: 1001}),
  //       knex('profile').insert({id: 602, name: 'bill', address: "2 somewhere street", city: "auckland", country: "New Zealand", postcode: 4502, user_id: 1002}),
  //       knex('profile').insert({id: 603, name: 'ray', address: "3 somewhere street", city: "auckland", country: "New Zealand", postcode: 4502, user_id: 1003})
  //     ]);
  //   });
};
